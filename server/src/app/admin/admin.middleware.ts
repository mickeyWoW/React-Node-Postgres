import { requestHandler } from 'lib/requestHandler';
import { Request } from 'express';

// todo
export const authAdmin = requestHandler({}, async (req, res, next) => {
  // await getCurrentAdmin(req as Request, res);
  // next();
});
